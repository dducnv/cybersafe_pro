import 'package:flutter/material.dart';

class PasswordGenarateTabletLayout extends StatelessWidget {
const PasswordGenarateTabletLayout({ super.key });

  @override
  Widget build(BuildContext context){
    return Container();
  }
}