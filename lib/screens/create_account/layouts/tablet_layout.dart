import 'package:flutter/material.dart';

class CreateAccountTabletLayout extends StatelessWidget {
const CreateAccountTabletLayout({ super.key });

  @override
  Widget build(BuildContext context){
    return Container();
  }
}