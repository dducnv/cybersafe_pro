import 'package:flutter/material.dart';

class CreateAccountDesktopLayout extends StatelessWidget {
const CreateAccountDesktopLayout({ super.key });

  @override
  Widget build(BuildContext context){
    return Container();
  }
}